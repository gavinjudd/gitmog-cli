/**
 * Every scorecard records the version that produced it, so a stored result stays
 * interpretable after the score contract changes (PLANNING.md §4.1).
 *
 * The numbering scheme itself is a Phase 1 decision.
 */
export const SCORING_VERSION = "0.0.0-foundation";
