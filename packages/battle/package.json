{
  "name": "@gitmog/battle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Orchestration: collect two public GitHub snapshots, score them, and build the battle. The one path every surface uses.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run"
  },
  "dependencies": {
    "@gitmog/github": "workspace:*",
    "@gitmog/personality": "workspace:*",
    "@gitmog/scoring": "workspace:*",
    "@gitmog/source-analysis": "workspace:*"
  },
  "devDependencies": {
    "@gitmog/config": "workspace:*",
    "@gitmog/test-fixtures": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
