# Synthetic calibration fixtures

Only purpose-written, license-safe fixtures belong here. Public repository source must not
be copied into this directory. Quality Judge product fixtures are added with the preview
implementation and validated by `pnpm quality:fixtures`.
